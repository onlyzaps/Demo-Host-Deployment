version: '3.8'

services:
  demoserver:
    build: .
    container_name: cs2_demo_server
    ports:
      - "3000:3000"
    environment:
      - STORAGE_DIR=/storage
    volumes:
      # Bind mount the host's /storage directory to the container's /storage directory
      - /storage:/storage
    restart: unless-stopped
